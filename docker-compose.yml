version: "3.9"

services:
  healthbook:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: healthbook
    restart: unless-stopped
    ports:
      - "8196:8196"
    volumes:
      # Persist the SQLite database and trained model outside the container
      - healthbook_data:/app/data
      - healthbook_model:/app/model
    environment:
      # ── Required for production ──────────────────────────
      SECRET_KEY: ${SECRET_KEY:-healthbook-dev-change-me}
      FLASK_DEBUG: ${FLASK_DEBUG:-false}

      # ── Google OAuth (get from console.cloud.google.com) ─
      GOOGLE_CLIENT_ID:     ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}

      # ── Firebase Phone Auth (get from console.firebase.google.com) ─
      FIREBASE_API_KEY:    ${FIREBASE_API_KEY:-}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8196/api/today-stats')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

volumes:
  healthbook_data:
  healthbook_model:
